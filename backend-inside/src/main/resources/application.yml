spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inside?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.inside.backendinside.aiPartner.domain
logging:
  level:
    com:
      inside:
        backendinside:
          aiPartner:
            mapper: debug
        aiPartner: debug
# Zhipu AI API Key Configuration
zhipuai:
  api-key: "c326b71813dc4c16af0a2f64c93b3fd5.8aQjTmiStyVA4BMp" # Please replace with your actual key