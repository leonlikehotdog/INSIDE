# INSIDE 心理疗愈小程序功能开发文档

## 1. 引言

本文档旨在详细阐述 INSIDE 心理疗愈小程序的各项功能模块，包括前端、后端（Java）和数据库（MySQL）的设计与实现细节。文档将重点关注数据流的输入与输出，确保各模块之间的数据交互清晰可追溯。

## 2. 总体架构概述

- **前端**: 微信小程序 (WXML, WXSS, JS)
- **后端**: Java Spring Boot
- **数据库**: MySQL

## 3. 功能模块详情

### 3.1 首页（Home）

**功能定位**: 每日哲思 + 推荐内容入口

#### 3.1.1 前端设计

- **页面结构**: 
  - 顶部横幅: 今日哲学卡片
  - 推荐内容区: 心理文章、冥想音频、哲学短片
  - 快捷入口: 情绪记录、测评、社区树洞
- **交互逻辑**: 
  - 点击哲学卡片可查看详情或分享。
  - 点击推荐内容可跳转至对应详情页。
  - 点击快捷入口可跳转至相应功能模块。

#### 3.1.2 后端 API 设计 (Java)

- **API 1: 获取今日哲学**
  - **接口**: `GET /api/home/philosophy`
  - **请求参数**: 无
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": {
        "id": "string",
        "text": "string",
        "author": "string"
      }
    }
    ```
  - **数据来源**: 数据库 `philosophy_quotes` 表
  - **数据流**: 后端从数据库读取哲学语录，返回给前端展示。

- **API 2: 获取推荐内容列表**
  - **接口**: `GET /api/home/recommendations`
  - **请求参数**: 
    - `type`: `string` (可选，如 "article", "meditation", "video")
    - `page`: `int` (页码)
    - `size`: `int` (每页数量)
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "id": "string",
          "title": "string",
          "coverImage": "string",
          "type": "string", // article, meditation, video
          "description": "string"
        }
      ]
    }
    ```
  - **数据来源**: 数据库 `articles`, `meditations`, `videos` 表
  - **数据流**: 后端根据请求类型和分页信息从数据库获取推荐内容，返回给前端展示。

#### 3.1.3 数据库设计 (MySQL)

- **表: `philosophy_quotes` (哲学语录表)**
  - `id`: `VARCHAR(36)` (主键)
  - `text`: `TEXT` (语录内容)
  - `author`: `VARCHAR(100)` (作者)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

- **表: `recommendations` (推荐内容总表)**
  - `id`: `VARCHAR(36)` (主键)
  - `title`: `VARCHAR(255)`
  - `cover_image_url`: `VARCHAR(255)`
  - `type`: `ENUM('article', 'meditation', 'video')`
  - `description`: `TEXT`
  - `content_url`: `VARCHAR(255)` (文章/视频/音频的具体内容链接)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

### 3.2 心情记录页（Mood Record）

**功能定位**: 记录每日情绪，促进自我觉察

#### 3.2.1 前端设计

- **页面结构**: 
  - 情绪选择: 图标式选择（如开心、焦虑、空虚）
  - 情绪评分: 滑动打分（1–10）
  - 心情日记: 文本输入区，可附图
  - 历史回顾: 查看过去记录趋势图
- **交互逻辑**: 
  - 用户选择情绪图标，滑动评分，输入日记内容，可上传图片。
  - 点击保存后，数据提交至后端。
  - 历史回顾区展示情绪趋势图和过往记录列表。

#### 3.2.2 后端 API 设计 (Java)

- **API 1: 提交心情记录**
  - **接口**: `POST /api/mood/record`
  - **请求参数**: 
    ```json
    {
      "userId": "string",
      "moodType": "string", // 开心、焦虑、空虚等
      "score": "int", // 1-10
      "diaryText": "string",
      "imageUrl": "string" // 可选
    }
    ```
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Mood record saved successfully."
    }
    ```
  - **数据流**: 前端收集用户输入，通过 API 提交给后端，后端将数据保存到数据库。

- **API 2: 获取心情历史记录**
  - **接口**: `GET /api/mood/history`
  - **请求参数**: 
    - `userId`: `string`
    - `startDate`: `string` (可选，YYYY-MM-DD)
    - `endDate`: `string` (可选，YYYY-MM-DD)
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "id": "string",
          "moodType": "string",
          "score": "int",
          "diaryText": "string",
          "imageUrl": "string",
          "recordDate": "string" // YYYY-MM-DD HH:mm:ss
        }
      ]
    }
    ```
  - **数据流**: 前端请求指定用户的历史记录，后端从数据库查询并返回。

#### 3.2.3 数据库设计 (MySQL)

- **表: `mood_records` (心情记录表)**
  - `id`: `VARCHAR(36)` (主键)
  - `user_id`: `VARCHAR(36)` (外键，关联用户表)
  - `mood_type`: `VARCHAR(50)` (情绪类型)
  - `score`: `INT` (情绪评分，1-10)
  - `diary_text`: `TEXT` (心情日记内容)
  - `image_url`: `VARCHAR(255)` (附图链接，可为空)
  - `record_date`: `DATETIME` (记录时间)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

### 3.3 心理测评页（Assessment）

**功能定位**: 自我评估与反馈

#### 3.3.1 前端设计

- **页面结构**: 
  - 测评列表: 焦虑量表、空心病量表、抑郁自评
  - 问题答题区: 单选/多选题
  - 分析报告: 图表展示 + 哲学引导语
  - 历史测评记录: 查看过往结果与变化趋势
- **交互逻辑**: 
  - 用户选择测评，进入答题页面。
  - 完成答题后，提交答案，后端计算结果并返回报告。
  - 历史记录页面展示所有已完成测评的简要结果。

#### 3.3.2 后端 API 设计 (Java)

- **API 1: 获取测评列表**
  - **接口**: `GET /api/assessment/list`
  - **请求参数**: 无
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "id": "string",
          "name": "string", // 测评名称
          "description": "string"
        }
      ]
    }
    ```
  - **数据流**: 后端从数据库获取所有可用测评列表，返回给前端。

- **API 2: 获取测评题目**
  - **接口**: `GET /api/assessment/{assessmentId}/questions`
  - **请求参数**: 
    - `assessmentId`: `string` (测评ID)
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "questionId": "string",
          "questionText": "string",
          "options": [
            {
              "optionId": "string",
              "optionText": "string",
              "score": "int" // 选项对应的分数
            }
          ],
          "type": "string" // single_choice, multi_choice
        }
      ]
    }
    ```
  - **数据流**: 后端根据测评ID从数据库获取题目和选项，返回给前端。

- **API 3: 提交测评答案并获取报告**
  - **接口**: `POST /api/assessment/{assessmentId}/submit`
  - **请求参数**: 
    ```json
    {
      "userId": "string",
      "answers": [
        {
          "questionId": "string",
          "selectedOptionIds": ["string"]
        }
      ]
    }
    ```
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": {
        "reportId": "string",
        "score": "int",
        "resultText": "string", // 分析报告文本
        "philosophyGuidance": "string", // 哲学引导语
        "chartData": {} // 图表数据，具体结构待定
      }
    }
    ```
  - **数据流**: 前端提交用户答案，后端计算总分，生成报告文本和哲学引导语，并将结果保存到数据库，最后返回报告给前端。

- **API 4: 获取历史测评记录**
  - **接口**: `GET /api/assessment/history`
  - **请求参数**: 
    - `userId`: `string`
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "reportId": "string",
          "assessmentName": "string",
          "score": "int",
          "resultSummary": "string",
          "submitDate": "string"
        }
      ]
    }
    ```
  - **数据流**: 前端请求指定用户的历史测评记录，后端从数据库查询并返回。

#### 3.3.3 数据库设计 (MySQL)

- **表: `assessments` (测评表)**
  - `id`: `VARCHAR(36)` (主键)
  - `name`: `VARCHAR(255)` (测评名称)
  - `description`: `TEXT` (测评描述)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

- **表: `assessment_questions` (测评问题表)**
  - `id`: `VARCHAR(36)` (主键)
  - `assessment_id`: `VARCHAR(36)` (外键，关联 `assessments` 表)
  - `question_text`: `TEXT` (问题内容)
  - `question_type`: `ENUM('single_choice', 'multi_choice')`
  - `order_num`: `INT` (问题顺序)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

- **表: `assessment_options` (测评选项表)**
  - `id`: `VARCHAR(36)` (主键)
  - `question_id`: `VARCHAR(36)` (外键，关联 `assessment_questions` 表)
  - `option_text`: `VARCHAR(255)` (选项内容)
  - `score`: `INT` (选项分数)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

- **表: `user_assessment_reports` (用户测评报告表)**
  - `id`: `VARCHAR(36)` (主键)
  - `user_id`: `VARCHAR(36)` (外键，关联用户表)
  - `assessment_id`: `VARCHAR(36)` (外键，关联 `assessments` 表)
  - `score`: `INT` (总分)
  - `result_text`: `TEXT` (分析报告文本)
  - `philosophy_guidance`: `TEXT` (哲学引导语)
  - `chart_data_json`: `JSON` (图表数据，存储为JSON字符串)
  - `submit_date`: `DATETIME` (提交时间)
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

### 3.4 我的页（Profile）

**功能定位**: 用户成长与个性化设置

#### 3.4.1 前端设计

- **页面结构**: 
  - 成长图谱: 打卡天数、情绪趋势、哲思完成度
  - 我的收藏: 喜欢的哲学卡片、文章、音频
  - 设置中心: 隐私设置、账号管理、意见反馈
- **交互逻辑**: 
  - 展示用户成长数据。
  - 点击收藏内容可查看详情。
  - 设置中心提供各项配置入口。

#### 3.4.2 后端 API 设计 (Java)

- **API 1: 获取用户成长数据**
  - **接口**: `GET /api/profile/growth/{userId}`
  - **请求参数**: 
    - `userId`: `string`
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": {
        "checkinDays": "int",
        "moodTrendData": {}, // 情绪趋势数据，具体结构待定
        "philosophyCompletionRate": "double"
      }
    }
    ```
  - **数据流**: 后端从数据库统计用户打卡天数、情绪记录，计算哲思完成度，返回给前端。

- **API 2: 获取用户收藏列表**
  - **接口**: `GET /api/profile/favorites/{userId}`
  - **请求参数**: 
    - `userId`: `string`
    - `type`: `string` (可选，如 "philosophy", "article", "meditation", "video")
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success",
      "data": [
        {
          "id": "string",
          "title": "string",
          "type": "string", // philosophy, article, meditation, video
          "coverImage": "string" // 可选
        }
      ]
    }
    ```
  - **数据流**: 后端从数据库查询用户收藏的内容，返回给前端。

- **API 3: 添加/取消收藏**
  - **接口**: `POST /api/profile/favorite`
  - **请求参数**: 
    ```json
    {
      "userId": "string",
      "contentId": "string",
      "contentType": "string", // philosophy, article, meditation, video
      "action": "string" // add, remove
    }
    ```
  - **响应**: 
    ```json
    {
      "code": 200,
      "message": "Success"
    }
    ```
  - **数据流**: 前端发送收藏/取消收藏请求，后端更新数据库中的收藏状态。

#### 3.4.3 数据库设计 (MySQL)

- **表: `users` (用户表)**
  - `id`: `VARCHAR(36)` (主键)
  - `username`: `VARCHAR(100)`
  - `avatar_url`: `VARCHAR(255)`
  - `created_at`: `DATETIME`
  - `updated_at`: `DATETIME`

- **表: `user_favorites` (用户收藏表)**
  - `id`: `VARCHAR(36)` (主键)
  - `user_id`: `VARCHAR(36)` (外键，关联 `users` 表)
  - `content_id`: `VARCHAR(36)` (被收藏内容的ID，如哲学语录ID、文章ID等)
  - `content_type`: `ENUM('philosophy', 'article', 'meditation', 'video')`
  - `created_at`: `DATETIME`

## 4. 数据流总结

- **内容发布**: 推荐内容（文章、冥想、视频）和哲学语录的发布，将通过后端管理系统（不在小程序范围内，但需预留API接口）录入数据库。小程序前端通过调用后端API获取这些内容。
- **用户数据**: 用户的心情记录、测评答案、收藏行为等数据，均由小程序前端收集后，通过后端API提交至后端，最终存储在MySQL数据库中。
- **数据展示**: 小程序前端通过调用后端API，从数据库获取用户个人数据、历史记录、统计报告等，进行可视化展示。

## 5. 后续计划

- 详细定义每个 API 的错误码和异常处理。
- 补充前端页面原型图和交互细节。
- 完善数据库字段的约束和索引设计。
- 考虑用户认证和授权机制。
- 考虑数据备份和恢复策略。